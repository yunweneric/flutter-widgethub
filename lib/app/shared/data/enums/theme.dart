/// Theme mode enumeration.
///
/// Currently commented out. Defines theme modes for the application
/// (system, dark, light). The application uses Flutter's built-in
/// [ThemeMode] instead.
// enum AppThemeMode { SYSTEM, DARK, LIGHT }
