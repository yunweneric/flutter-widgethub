/// Centralized route name constants for the application.
///
/// This class provides a single source of truth for all route paths used
/// throughout the application. Using these constants ensures route names
/// are consistent and easier to maintain.
class RouteNames {
  /// Home page route.
  static const home = "/";

  /// Base components route.
  static const components = "/components";

  /// Introduction/getting started route.
  static const introduction = "/components/introduction";

  /// Blocks category route.
  static const blocks = "/components/blocks";

  /// Templates category route.
  static const templates = "/components/templates";

  /// Animations category route.
  static const animations = "/components/animations";

  /// Effects category route.
  static const effects = "/components/effects";

  /// Get started guide route.
  static const getStarted = "$introduction/get-started";

  /// Request component route.
  static const requestComponent = "$introduction/request-component";

  /// Text components route.
  static const texts = "$blocks/texts";

  /// Button components route.
  static const buttons = "$blocks/buttons";

  /// Chip components route.
  static const chips = "$blocks/chips";

  /// Input components route.
  static const inputs = "$blocks/inputs";

  /// Loader components route.
  static const loaders = "$blocks/loaders";

  /// Switcher components route.
  static const switchers = "$blocks/switchers";

  /// Slider components route.
  static const sliders = "$blocks/sliders";

  /// Bottom navigation bar components route.
  static const bottomBars = "$blocks/bottom-nav-bars";

  /// Drawer components route.
  static const drawers = "$blocks/drawers";

  /// Carousel components route.
  static const carousels = "$blocks/sliders-and-carousels";

  /// Authentication screen templates route.
  static const authScreens = "$templates/authentication-screens";

  /// Home screen templates route.
  static const homeScreens = "$templates/home-screens";

  /// Onboarding screen templates route.
  static const onboardingScreens = "$templates/onboarding-screens";

  /// App clone templates route.
  static const appClones = "$templates/app-clones";

  /// Playground route.
  static const playground = "/components/playground";

  /// Not found/404 error route.
  static const notFound = "not-found";

  /// Hover animation components route.
  static const hoverAnimations = "$animations/hover-animations";

  /// Skew animation components route.
  static const skewAnimations = "$animations/skew-animations";

  /// Rotation animation components route.
  static const rotationAnimations = "$animations/rotation-animations";

  /// Gradient effect components route.
  static const gradientEffects = "$effects/gradient-effects";

  /// Background effect components route.
  static const backgroundEffects = "$effects/background-effects";
}
